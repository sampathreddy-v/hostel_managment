# Project Rules

- **Local Development Only**: All code changes and server operations must be executed strictly on the local environment/server.
- **No Remote Git Pushes**: Do NOT execute `git push` or push any code changes to GitHub or any remote repository until explicit permission/instruction is given by the user.
